package cn.metaq.sqlbuilder.constants;

public enum SqlBuilderStepType {

    TABLE,
    FILTER,
    DISTINCT,
    LEFT_JOIN,
    RIGHT_JOIN,
    INNER_JOIN,
    UNION,
    EXCEPT,
    COUNT,
    SUM,
    GROUP_CONCAT;
}
